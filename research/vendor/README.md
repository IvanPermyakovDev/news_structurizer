Vendor / large third-party sources that should not be on the product import path.

Currently contains a vendored copy of HuggingFace Transformers moved from the repo root
to avoid shadowing the real `transformers` Python package.

